{
    "alias": "workflow",
    "version": "0.1",
    "requirements": ["department"],
    "plugins": {
        "action_end_init": ["ONESWorkflowInit"]
    },
    "navs": {
        "settings": {
            "childs" : {
                "workflow": "workflow/list/workflow"
            }
        }
    }
}