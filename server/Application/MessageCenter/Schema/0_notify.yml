notification:
    created:
        type: datetime
        default: CURRENT_TIMESTAMP
    status:
        type: integer
        limit: 1
    $meta:
        foreign:
            user_info:
                field: to_user_id